# Template for backend/local.env — copy to local.env and fill in your key.
#   cp local.env.example local.env
# local.env is gitignored; this example file is committed for reference.

ANTHROPIC_API_KEY=sk-ant-...

# Optional model overrides (defaults are valid public Anthropic IDs):
# CHEAP_MODEL=claude-haiku-4-5
# STRONG_MODEL=claude-sonnet-4-6

# Optional custom API base URL (leave unset to hit Anthropic directly):
# ANTHROPIC_BASE_URL=
